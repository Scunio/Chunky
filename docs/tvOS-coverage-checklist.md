# tvOS coverage checklist — overnight loop

Goal: every reachable tvOS screen/submenu actually exercised with real remote input (key_press D-pad + select via xcodebuildmcp), not just read as code or rendered once in Canvas. Mark `[x]` only after real interaction confirmed a screen works (focus reaches everything, no dead ends, no visual defects) — not after just opening it once.

Update this file every iteration: check off what got real coverage, add a one-line note for anything found (bug filed separately in tvOS-design-patterns.md, or "fine" if verified clean).

## Libreria tab
- [x] Grid: focus a cell, open detail (`TVComicDetailView`)
- [x] Detail: "Leggi" opens reader
- [x] Detail: Menu (`onExitCommand`) pops back to the grid — FOUND AND FIXED a real bug: previously Menu on `TVComicDetailView` skipped the grid entirely and exited straight to the tvOS Home Screen (reproduced live, twice, before the fix). Root cause (confirmed via code, not guessed): both the grid and the detail screen hide their navigation bar (`.toolbar(.hidden, for: .navigationBar)`), leaving tvOS no visible "non-root" affordance to base Menu's default pop-vs-exit decision on, so it fell through to app-exit at every level. Fixed with an explicit `.onExitCommand { dismiss() }` on `TVComicDetailView` (the only `.navigationDestination` push in the app — checked, no other screen has this pattern). Verified live after the fix: Menu from the detail screen now lands back on the grid, not Home.
- [x] Detail: "Riprendi" (re-open an in-progress comic) resumes at the correct page — confirmed live: opened `uitest-grid-06`, advanced to page 2/3, exited via Menu, re-opened via "Riprendi" — landed back on 2/3, not reset to 1. Also incidentally confirmed Menu-exit from a normal mid-read state works correctly (separate checklist item below).
- [x] Grid scrolling with many items — seeded 14 comics via `--uitesting-seed-multi`, real grid confirmed, scrolls cleanly via D-pad Down, focus highlight visible, titles never covered/clipped, tab bar correctly hides once scrolled past the top (same as elsewhere tonight)
- [ ] Empty-library state (verified once, not re-checked after other fixes)

## In lettura tab
- [x] `TVNowReadingTabView` — "Riprendi" button reachable via tab-bar right-navigation from Libreria, cover/title/progress/pill all clean, single shape

## Preferiti tab
- [x] Empty state — confirmed live: removed the one existing favorite via the grid tile's `.contextMenu` → "Rimuovi dai preferiti" (reached with a held Select, `duration: 1.2`, mirroring the earlier long-press-to-add path), Preferiti tab then showed a "Nessun preferito" card — FOUND AND FIXED a real bug (user-reported after this checklist item was first marked done): the card was hugging the top-left corner instead of centering in the available space. Root cause and fix in tvOS-design-patterns.md. Verified live after the fix: card now centers properly, no crash.
- [x] With at least one favorite — found and exercised a second, tvOS-reachable favorite-toggle path that doesn't need `ComicInfoSheet` (which is confirmed unreachable, see Reader section below): the library grid tile's `.contextMenu` (`LibraryView.swift:917-925`, "Aggiungi ai preferiti"), opened live via long-select (key_press select held ~1.2s) on a focused tile — menu opened, item selected, tile showed a yellow favorite-star badge. Physically visited the Preferiti tab afterward and — FOUND AND FIXED a real bug: it showed ALL 15 comics, not just the 1 favorite, even though `filteredComics` itself (confirmed via a temporary debug print) was correctly computing just the 1-item favorites-filtered result. Root cause: `libraryGrid`'s "Novità" (recent-comics) section (`LibraryView.swift:549-551`) was shown unconditionally on every tvOS tab, pulling from `recentComics` — a property unrelated to `effectiveSelection`/favorites — so it listed non-favorite comics on the Preferiti tab regardless of the correct filter underneath it. Fixed by gating that section to `effectiveSelection == .all` (the unfiltered Libreria tab only). Verified live after the fix: Preferiti now shows exactly the 1 favorited comic ("ALTRI FUMETTI" / "1 fumetti"). Rebuilt clean on tvOS/iOS/macOS (fix is tvOS-gated, iOS/macOS confirmed unaffected by code review — the "Novità" section only exists inside `#if os(tvOS)`).

## Account tab
- [x] Illustration panel + list render (Canvas + simulator)
- [~] "Downloads" row → `DownloadsView` — empty state confirmed live, title-in-layout fix holds (no overlap), "Stop all" pill clean. FOUND AND FIXED a real, serious bug on this round's critical re-check: Menu from this screen (empty state) exited the *entire app* to the tvOS Home Screen instead of popping back to the Account list — reproduced live, confirmed via screenshot (app icons, not the Account screen). Root cause and fix (`.focusable(true)` + `.onExitCommand { dismiss() }` on the screen root) documented in full in tvOS-design-patterns.md; the same missing-affordance bug and fix were then found and applied to `LocalUploadView`, `RemoteBrowserView`, and `AddAccountView` too — see their own entries below. Re-verified live twice in a row after the fix: Menu now correctly pops back to the Account list. NOT tested: an actual download in progress (needs a real transfer, none available in this environment)
- [x] "Web" row → `LocalUploadView` — FOUND AND FIXED the same Menu-exits-the-whole-app bug as `DownloadsView` (see above), same fix applied, verified live: Menu now correctly pops back to the Account list instead of exiting. Still not fully exercised beyond that — never toggled server on/off with select, never checked the IP-address rows that appear when running
- [!] "I tuoi account" → existing account → `RemoteBrowserView` — BLOCKED, not an app bug: creating a test account needs typing a URL, and the tvOS on-screen keyboard can only be driven character-by-character via D-pad focus navigation between keys (`type_text` fails to focus the field here, same as the parental-lock passcode field; confirmed again this round via real per-key D-pad navigation attempt — `Down` from the focused "a" key jumps to the language-switcher icon, not to "b", so there's no scripted path through it at all). Scripting a reliable letter-by-letter path through the keyboard grid is impractically expensive for this loop — skipping rather than burning the budget on it. Would need either a seeded fake `RemoteAccountEntity` (no launch-arg support for that today) or real per-key navigation on a real remote/device. Received the same Menu-exits-the-app fix as `DownloadsView`/`LocalUploadView` by code inspection (identical structure, same hidden-bar parent `NavigationStack`) but not independently live-verified for the same reason it can't be reached at all.
- [ ] `RemoteBrowserView` subfolder recursion (push into a folder inside a folder) — same blocker
- [x] "Calibre / Ubooquity / OPDS" → `AddAccountView` OPDS form — reached live (tab bar Right-cycling worked reliably this round, see note below), fully navigated Tipo→Nome→URL→Nome utente→Password→3 toggles (Scansione automatica/Cartelle Smart/Pre-cache)→Salva, all reachable, single clean pills throughout, no dead ends. "Salva" with an empty required URL showed the same friendly validation error as WebDAV ("Inserisci un URL valido, comprensivo di http:// o https://"), no crash.
- [x] "Nuovo account WebDAV" → `AddAccountView` WebDAV form — fully navigated top to bottom (Tipo→Nome→URL→Nome utente→Password→3 toggles→Salva, 7 real Down presses), all reachable, no dead ends, single-shape pills throughout, "Salva" clean
- [x] "Nuovo account SMB" → `AddAccountView` SMB form — opened correctly, real network-discovered server ("NAS07BE7B") showed up under "Trovati in rete", all fields navigable
- [x] `AddAccountView` "Avanzate" disclosure — confirmed live: select expands (chevron flips, "Porta"/"Workgroup" appear, Porta pre-filled "445") and collapses again cleanly on a second select, single clean pill throughout, no double-shape
- [x] `AddAccountView` toggle rows (Automazione: Scansione automatica/Cartelle Smart/Pre-cache) — confirmed focusable and reachable in sequence (not yet select-toggled to confirm the On/Off flip itself, only focus-reachability)
- [x] `AddAccountView` "Salva" with empty required field → friendly validation error shown ("Inserisci un URL valido, comprensivo di http:// o https://"), not a crash/raw error
- [x] `AddAccountView` Menu exit — FOUND AND FIXED the same Menu-exits-the-whole-app bug as `DownloadsView` (see the "Downloads" entry above) — this view already hid its own nav bar too (`.toolbar(.hidden, for: .navigationBar)` in `tvOSForm`), on top of being pushed under the Account tab's own hidden-bar `NavigationStack`. Same `.onExitCommand { dismiss() }` fix (it already had `@Environment(\.dismiss)` declared, just unused for this). Verified live: Menu from the SMB form now correctly pops back to the Account list instead of exiting.
- [ ] `AddAccountView` "Salva" with valid data — actually saves and returns to Account list with the new entry visible (not tested — would need a fake but well-formed URL)
- [ ] Real connection attempt: no live server available in this environment — the friendly-error path for a syntactically-valid-but-unreachable address (as opposed to a malformed one) is still untested

## Impostazioni tab
- [x] `ColorThemeView` — Modalità segmented control: right-navigation works fine here (unlike the reader header — confirms that's a reader-specific bug, not a tool limitation), select on "Scura" visibly flipped the whole app to dark, confirmed real
- [x] `ColorThemeView` — palette rows (Predefinito/Seppia/Notte) — reachable and selectable. Note: within this specific row, Down moves between the palette options, not Right (unlike every other segmented-looking control tonight, which all used Right) — not fully understood why, but confirmed consistent and real (not a hash artifact) across repeated tries; use Down here specifically.
- [x] `ColorThemeView` — "Sfondo pagina" pushes its own picker screen (Automatico/Nero/Bianco); selecting "Bianco" genuinely updated the parent row's displayed value from "Nero" to "Bianco" — confirmed real, not just focus.
- [x] `ColorThemeView` — tint pills (Nessuno/Seppia/Notte) — reachable and selectable (same Down-to-navigate pattern as the palette row above), select on "Notte" registered without a crash. Effect only visible in the reader itself, not re-verified there this round.
- [~] `ParentalLockSettingsView` — reached live, "Nuovo codice"/"Conferma codice"/"Salva codice" all single clean pills, select opens the tvOS on-screen keyboard correctly and Menu dismisses it cleanly back to the form (no crash/glitch). NOT fully tested: couldn't actually type a code (type_text tool fails to focus the field on tvOS — same root cause as the tap/orientation-probe issue, a tool limitation not an app bug) or reach the auto-lock trigger picker (below the fold). While reading the code for this: FOUND AND FIXED a real (if minor) bug unrelated to the typing blocker — the "Sblocca con Face ID / Touch ID" toggle (shown once a passcode exists) had no tvOS exclusion, so it would have appeared on a platform with no biometrics API at all (`ParentalLock.swift`'s own `unlockWithBiometrics` already no-ops on tvOS); toggling it on tvOS would silently do nothing useful. Gated with `#if !os(tvOS)`. Also fixed a stale comment in `ParentalLock.swift:73` that claimed this settings screen "isn't reachable" on tvOS — it is (confirmed live tonight and via `TVRootTabView.swift:110`'s `NavigationLink`); the comment predated that screen being wired up. Checked for the same pattern elsewhere (`ParentalLockGateView.swift`'s biometrics button is already correctly gated behind the now-tvOS-unreachable toggle, no separate fix needed there). Rebuilt clean on tvOS/iOS/macOS.
- [ ] Parental lock gate itself: lock the app, confirm the gate blocks focus into the tab bar underneath (code-fixed, never exercised live)

## Reader (open any comic to reach)
- [x] Page turn left/right via D-pad
- [x] Header reachable via Up after the focus-trap fix — reconfirmed 4x across fresh app launches, robust
- [x] Header: moving focus between icons via right — FIXED and verified live after TWO compounding root causes (both found via `print()` + `runtimeLogPath`, not screenshots): (1) `.onMoveCommand` kept firing on the pager as long as it stayed `.focusable(true)` regardless of any `@FocusState` value change; (2) with only "Libreria" bound to the shared FocusState, moving to any other unbound button reverted the state to `nil`, which re-enabled the pager's focusability mid-navigation. Fix: every header button now has its own `ReaderFocusTarget` case and its own `.focused(...)` binding — see tvOS-design-patterns.md for the full mechanism. Verified: Right after Up no longer turns the page, and Select on the search icon reliably opens `ReaderFindView` for real (2-3s settle time needed between scripted key presses — see the testing-caveat note in the patterns doc, this is a simulator-automation timing quirk, not an app bug real remote users would hit).
- [x] Header: search icon → `ReaderFindView` — confirmed opens (query field + "Fine" button + hint text all present); "Fine"/Menu closes it cleanly back to the reader, no crash. NOT tested: typing a query and jumping to a match (same `type_text`-can't-focus-a-tvOS-field tool limitation as the parental-lock passcode field)
- [x] Header: "Novità" popover — confirmed live, own UI appeared ("Nuovi" title + "Cancella" button), Menu closes it cleanly back to the reader with focus preserved on the same icon
- [x] Header: "Ora in lettura" popover — confirmed live, own UI appeared ("Ora in lettura" title + "2 / 3" progress), closes cleanly
- [x] Header: Account icon → sheet — confirmed live, `AccountsView` opened correctly over the reader (Account title, Downloads/Web/OPDS/WebDAV/SMB rows all present) — no nested-NavigationStack crash/glitch, closes cleanly
- [x] Header: layout-cycle icon — confirmed live, select cycled a.square → square (icon changed) and the page display genuinely switched from double-page ("2–3 / 3") to single-page ("2 / 3")
- [x] Header: Down returns functional control to page-turning — FOUND AND FIXED a real bug (user-reported): from a header button (e.g. "Libreria", reached via Up), Down never actually returned control to the pager. Root cause and fix documented in full in tvOS-design-patterns.md — a focused `Button` never gives up real UIKit focus even when `.focusable` toggles false (4 different techniques tried and failed live). Fix: every header button's `.onMoveCommand` now proxies Left/Right page-turns directly via a shared `handleHeaderMoveCommand`, instead of waiting for a focus hand-off that never arrives. Verified live end to end: page counter genuinely changed 3/3 → 2/3 → 3/3 while focus was still (per the log) nominally on the Libreria button. Applied identically to all 6 header buttons (same shared function).
- [x] Header: re-verification after the Down fix above — FOUND AND FIXED a self-inflicted regression the same round: the first version of that fix proxied Left/Right unconditionally, which broke ordinary Right-navigation *between* header icons (confirmed live: Right from "Libreria" toward the search icon silently turned the page instead of just moving focus, surfacing as the "Fine del fumetto" dialog popping since it happened to be the last page). Fixed by gating the page-turn proxy behind a new `isAwaitingPagerReturn` flag, set only by an explicit Down. Re-verified live end to end after the fix: Right from "Libreria" now correctly moves focus to the search icon with no page-turn (3/3 stayed 3/3), and Down→Left still turns the page (3/3 → 2/3). Full detail and the underlying `.onMoveCommand`-firing-unconditionally lesson in tvOS-design-patterns.md.
- [!] Long-press title → `ComicInfoSheet` — CONFIRMED unreachable via remote on tvOS: the title `Text` has no `.focusable`/`.focused` wiring at all (checked the code directly), so it's outside the tvOS focus system entirely; `.onLongPressGesture` has nothing to attach to without focus. This isn't a focus-navigation bug like the header one — it's a genuinely missing access point: there is currently NO way to reach "Vai a pagina" or toggle reading direction from the reader on tvOS. (Favorite-toggle specifically is NOT blocked — see Libreria tab / Preferiti tab notes: the library grid tile's `.contextMenu` reaches the same `isFavorite` flag from outside the reader.) Needs a product decision (where to add an entry point for "Vai a pagina"/reading-direction — header's at 6 icons already, adding a 7th contradicts the original "no extra icon" design intent behind putting this on a long-press in the first place), not just a code fix — flagging for the user rather than picking a design unilaterally.
- [x] End of comic → `nextComicConfirmation` card — FOUND AND FIXED a second instance of the header's focus bug here (dialog buttons had no `.focused` binding, same nil-reversion mechanism — see tvOS-design-patterns.md). Both paths confirmed live after the fix: "Annulla" dismisses the card and page-turning resumes normally; "Apri" switches to the next comic (title/page counter updated to the new comic, reset to page 1).
- [x] Exit via Menu (`onExitCommand`) — confirmed clean from normal mid-read state (header focused, no dialog). Also confirmed live with the end-of-comic dialog open: opened `uitest-grid-12` (already "Letto", landed on 3/3), pressed Right to trigger `nextComicConfirmation` ("Fine del fumetto" / "Continuare con: uitest-grid-13"), pressed Menu — dialog and reader both closed in one step, landed back cleanly on the `TVComicDetailView` for grid-12 with "Leggi" focused, no crash/stuck state. Find-open Menu-exit was verified earlier tonight (not re-verified after today's fixes, but no code touched find's Menu handling since).

## Status: essentially complete, remaining items blocked by tooling (not app bugs)

This round found the reliable way to reach the Account tab (fresh launch + repeated `Right` presses — see the correction note in tvOS-design-patterns.md), which unblocked the OPDS form (now fully tested, checked off above) and the Preferiti empty state (also now checked off, via the grid tile's held-Select context menu to remove the one favorite live).

What's still genuinely open, and why:
- **`AddAccountView` "Salva" with valid data** — needs typing a real URL. Tried both `type_text` (fails to focus the field, as already documented) and real D-pad navigation through the on-screen keyboard itself (new attempt this round): `Down` from the focused "a" key jumps to the language-switcher icon, not to "b" — confirmed this keyboard isn't a simple navigable list, so there's no scripted path through it at all, not just an unreliable one.
- **`RemoteBrowserView` / subfolder recursion** — same typing blocker, needed to create a test account with a real address.
- **Parental lock gate** — same typing blocker, needed to actually set a passcode first.
- **Library empty state** — not re-blocked, just skipped this round: the only way to get there is clearing the seeded 15-comic fixture, which isn't worth doing since it's shared by other still-useful tests and there's no dedicated empty-seed launch arg.

None of these are app bugs — they're one shared tooling limitation (the tvOS on-screen keyboard has no scriptable D-pad path in this environment). Needs a physical Apple TV/real remote, or `type_text` support for tvOS fields in a future xcodebuildmcp/AXe release.

**This round's critical-review pass** (re-checking already-`[x]` items with a more skeptical eye, per explicit user request after two real bugs were found live rather than by the loop): re-tested the Reader header's Down fix end-to-end and caught a real self-inflicted regression it had introduced the same session — the Left/Right page-turn proxy fired unconditionally, breaking ordinary Right-navigation between header icons (confirmed live: Right toward the search icon silently turned the page too). Fixed with an explicit `isAwaitingPagerReturn` state flag; re-verified live that both behaviors now work correctly (icon navigation stays a pure focus move, page-turning only kicks in after Down). Also grepped the codebase for every other `ContentUnavailableView` for the earlier centering bug pattern: none affected (`TVPanel` uses `.center` alignment, `TVRootTabView`'s sits in a bare `Group`). Attempted a live check of `NewComicsView`'s empty popover but couldn't get it to open this round (Select flakiness, 3 tries) — left as code-reviewed-safe rather than live-confirmed.

**This round's Menu-exit audit, following the same rigorous method (press Menu, screenshot the actual result) that caught the Account-tab bug**: re-tested `ColorThemeView` and `ParentalLockSettingsView` (Impostazioni tab) live — both confirmed clean, Menu correctly pops back to the Impostazioni list (screenshot-confirmed both times, not just inferred). Also tested `TVNowReadingTabView` ("In lettura" tab) at its own root with nothing pushed: Menu there did exit to the Home Screen — but this is a genuine tab root with no parent to pop to, which matches expected/conventional tvOS behavior (same as pressing Menu at any tab's unpushed root), not the same bug class as the Account-tab screens (which *were* pushed one level deep and should have popped, not exited). Attempted to re-verify the Reader's popovers (search/Novità/Ora-in-lettura/Account-sheet) for the same risk, since `AccountsView` presented as a sheet from the Reader still internally uses the same hidden-bar `NavigationStack` as the Account tab — but couldn't get any of them to open this round after ~8 Select/Right attempts (severe input flakiness, screenshots confirmed focus was correctly on the target icon each time, Select just didn't register). Left unverified live; sheets have different Menu-dismiss semantics from pushed `NavigationStack` content on tvOS (a presented sheet's own Menu-dismiss is generally handled by the system independent of internal nav-bar visibility), so the risk is judged lower-confidence but not ruled out — worth a real re-check in a future session once input is more cooperative, rather than a code-only judgment call.

**Earlier in this round: FOUND AND FIXED a much more serious bug** — Menu from any screen pushed under the Account tab (`DownloadsView`, `LocalUploadView`, `RemoteBrowserView`, `AddAccountView`) exited the *entire app* to the tvOS Home Screen instead of popping back one level, reproduced live. Same root cause as the earlier `TVComicDetailView` fix (a hidden nav bar with no visible pop affordance), but this time compounded by a screen that could end up with literally nothing focusable, which meant `.onExitCommand` alone wasn't enough — needed an explicit `.focusable(true)` on the screen root too. Full root-cause writeup, including a debugging dead-end worth remembering (the tab bar's highlight state does NOT reliably indicate where real focus is — several fix attempts were wrongly declared failed based on that alone), in tvOS-design-patterns.md. Fix verified live on three of the four affected screens; the fourth (`RemoteBrowserView`) got the identical fix by code inspection but couldn't be independently live-verified since reaching it needs a saved account, blocked by the same on-screen-keyboard limitation as everything else in this section.

## Legend
`[x]` = really tested live · `[~]` = partially tested, gaps noted · `[!]` = blocked on an unresolved question (see note) · `[ ]` = not tested yet
